# 서버 환경 변수 (모두 선택)
PORT=8787
# 세계 기록·신고·이벤트 로그를 저장할 폴더
DATA_DIR=./.data
# 설정하면 이 코드를 아는 사람만 들어올 수 있다. 초대 링크에는 자동으로 담긴다
ACCESS_CODE=
# 화면에 보일 서비스 이름. 비우면 NOVA POUCH. 외부 공개 때는 가칭을 넣는다
APP_BRAND=
# 프록시(Fly.io 등) 뒤에서 실제 접속 주소를 읽을 때 1
TRUST_PROXY=0
